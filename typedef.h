#ifndef TYPEDEF_H
#define TYPEDEF_H

#include <stdio.h>
#include <stdlib.h>

#define n 4

int j, i,i_first_pp, j_first_pp, i_last_dd, j_last_dd;
int first_pp, last_dd, buff;

void rand_masiv(int **masiv);
void sort_masiv(int **masiv);
void input_masiv(int **masiv);
void output_masiv(int **masiv);
void target_masiv(int **masiv);

#endif
